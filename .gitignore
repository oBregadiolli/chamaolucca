# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Notas/documentação de dev pessoais do Brega (não versionar)
.joao/

# Claude Code — settings locais (mapeiam o PAT pessoal do Supabase; não versionar)
.claude/settings.local.json
# Claude Code — config local do projeto (escopo local do MCP grava token literal aqui; não versionar)
.claude.json

# Environment / secrets — NEVER commit
.env
.env.*
!.env.example

# Internal audit docs — never commit
April/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI local state
supabase/.temp/

# E2E / Playwright artifacts (generated locally)
scripts/e2e-prod-report/
playwright-report/
test-results/
blob-report/
.playwright/
tmp-*.html
